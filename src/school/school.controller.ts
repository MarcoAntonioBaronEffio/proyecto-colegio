import { Body, Controller, Post } from '@nestjs/common';
import { SectionService } from 'src/section/section.service';

// 🏛️ Ruta base del controlador -> /sections
@Controller('school')
export class SchoolController {

    

}
